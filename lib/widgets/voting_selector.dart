/*import 'package:eurovisionvoteapp/widgets/countryListPage.dart';
import 'package:flutter/material.dart';
import 'package:flutter/src/foundation/key.dart';
import 'package:flutter/src/widgets/framework.dart';

import '../services/firebase_voting.dart';

class VotingSelector extends StatefulWidget {
  const VotingSelector({Key? key}) : super(key: key);

  @override
  State<VotingSelector> createState() => _VotingSelectorState();
}

class _VotingSelectorState extends State<VotingSelector> {
  @override
  Widget build(BuildContext context) {
    return StreamBuilder(
      stream: readVote(),
      builder: (context, snapshot) {
        final votes = [];
        if (snapshot.hasData) {}
      },
    );
  }
}*/
